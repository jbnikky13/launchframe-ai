import type { GeminiCreative } from './gemini';
import type { AudioPlan } from './audio';
export type MediaMotion='push-in'|'pull-out'|'pan'|'parallax'|'static';
export type MediaTransition='fade'|'slide'|'zoom'|'cut'|'match-cut';
export type MediaScene={id:string;prompt:string;useSourceAsset:boolean;motion:MediaMotion;transition:MediaTransition;narration:string;onScreenText:string;duration:number;visualMode?:string};
export type MediaDirection={scenes:MediaScene[];audio:AudioPlan};
export function buildMediaDirection(creative:GeminiCreative,audio:AudioPlan):MediaDirection{
 const scenes=creative.scenes.map((s:any,i)=>{const mode=s.visualMode||(/source|screenshot|interface|dashboard|product screen/i.test(s.visual)?'source':'image');const motion:(MediaMotion)=s.camera==='pan-left'||s.camera==='pan-right'?'pan':(s.camera||(['push-in','pan','parallax','pull-out'][i%4] as MediaMotion));const transition=s.transition||(['fade','match-cut','zoom','cut'][i%4] as MediaTransition);return{id:`scene-${s.order||i+1}`,prompt:`${s.visual}. Visual mode: ${mode}. Style: production-ready cinematic. Subject, composition, environment, lighting, camera action and continuity must be explicit. Preserve product identity and do not invent UI claims.`,useSourceAsset:mode==='source',motion,transition,narration:s.narration,onScreenText:s.onScreenText,duration:s.duration,visualMode:mode}});return{scenes,audio};}